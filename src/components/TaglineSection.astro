---
---

<section class="tagline-section">
  <div class="tagline-wrap">
    <h2 class="tagline-text">
      <span class="tagline-line1">Transformamos&nbsp;</span><span class="tagline-line2">tu estilo</span>
    </h2>
  </div>
</section>

<style>
  .tagline-section {
    background-color: #000;
    padding: 6rem 2rem;
    text-align: center;
  }

  .tagline-wrap {
    max-width: 900px;
    margin: 0 auto;
  }

  .tagline-text {
    display: flex;
    flex-direction: row;
    align-items: baseline;
    justify-content: center;
    flex-wrap: wrap;
    line-height: 1.05;
  }

  .tagline-line1 {
    font-family: 'Barlow Condensed', sans-serif;
    font-weight: 900;
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #fff;
  }

  .tagline-line2 {
    font-family: 'Barlow Condensed', sans-serif;
    font-weight: 900;
    font-size: clamp(2.5rem, 7vw, 5.5rem);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #ffadd0;
  }
</style>
